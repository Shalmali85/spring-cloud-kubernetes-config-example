logging.level.org.springframework.cloud.kubernetes: DEBUG
spring.application.name: config-kubernetes

management:
  endpoints:
    web:
      exposure:
        include: info,refresh,keepalive,health

